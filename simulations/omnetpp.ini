[General]
network = bfsroutingproject_new.BFSRoutingNetwork
sim-time-limit = 20s

# Enable logging
**.cmdenv-log-level = info

# Display settings for Qtenv
qtenv-default-config = 
qtenv-default-run = 1

# Animation speed - slower for better visualization
**.animationSpeed = 1

# Network visualization
**.displayString = "i=block/routing"